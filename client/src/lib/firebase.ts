/**
 * NOTE: This file is kept for backward compatibility.
 * The application has been migrated to use direct Google OAuth instead of Firebase.
 * See googleAuth.ts for the current implementation.
 */

// Placeholder export to avoid import errors in other files that might still reference this
export const auth = null;
